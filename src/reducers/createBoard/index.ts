import { combineReducers } from 'redux';
import afterAddListModal from 'reducers/createBoard/afterCloneAddList';
const createBoardReducer = combineReducers({ afterAddListModal });

export default createBoardReducer;
